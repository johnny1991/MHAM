#!/bin/bash

###################################### PARAMETERS ##############################################

. HA.conf
. $(dirname $0)/HA.conf

printer='script_perso'

echo -e "\033[32m $printer[info] Connexion en ssh a $ip_magento \033[0m"
echo -e "\033[32m $printer[info] lecture de l'adresse ip dans le local.xml \033[0m"

ip=$(sudo ssh -T root@$ip_magento "xmlstarlet sel -t -m '/config/global/resources/default_setup/connection' -v host $local_xml")

for (( i = 0; i < ${#ip_bdd[@]}; i++ )); do
    if [ ${ip_bdd[$i]} != $ip ]; then
        Slave_IO_Running=`mysql -h $ip -u $db_user -p$db_password $db_name -N -s -e 'SHOW SLAVE STATUS\G' | grep 'Slave_IO_Running' | awk -F': ' {'print $2'}`
        Slave_SQL_Running=`mysql -h $ip -u $db_user -p$db_password $db_name -N -s -e 'SHOW SLAVE STATUS\G' | grep 'Slave_SQL_Running'| awk -F': ' {'print $2'}`
        echo "mysql -h $ip -u $db_user -p$db_password $db_name -N -s -e 'SHOW SLAVE STATUS\G' | grep 'Slave_IO_Running' | awk -F': ' {'print $2'}"
        echo $Slave_IO_Running
        if [ -z "$Slave_IO_Running" ] && [ -z "$Slave_SQL_Running" ]; then
            if [ $Slave_IO_Running = No ] || [ $Slave_SQL_Running = No ]; then
                echo PROBLEME DE SYNCHRONISATION
            fi
        fi
    fi
done